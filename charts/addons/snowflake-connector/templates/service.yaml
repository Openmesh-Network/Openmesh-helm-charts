apiVersion: v1
kind: Service
metadata:
  name: {{ include "snowflake-connector.fullname" . }}
  labels:
    {{- include "snowflake-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snowflake-connector.selectorLabels" . | nindent 4 }}
