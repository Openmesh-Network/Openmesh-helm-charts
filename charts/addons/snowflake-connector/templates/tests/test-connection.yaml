apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snowflake-connector.fullname" . }}-test-connection"
  labels:
    {{- include "snowflake-connector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snowflake-connector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
